# v1 Index: Web UX Parity

Date: 2026-03-02

## Scope

- 聚焦 `/` 命令面板与补全相关交互，让 Web 操作习惯更贴近 Claude Code。

## Plans

- `v1-web-ux-parity.md`

## DoD (hard)

- `npm test`（即 `node --test`）全绿
- Web 端在输入 `/m` + `Tab` 时，能看到面板/补全输出（人工验证步骤记录在 v1 plan）

## Ship status

- 已本地提交：每轮迭代建议一个 commit；若需要 push，请先配置 git remote（当前仓库可能未设置 remote）。
