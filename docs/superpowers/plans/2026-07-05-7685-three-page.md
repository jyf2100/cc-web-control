# :7685 三页面重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** :7685 hub UI 重构为三页面 —— ①看板(监控**被控**按机分组 + 卡片多选 + 扇出 bar;**主控不进 fleet,看板不展示主控——监控/管理归多机控制台**)②多机控制台(主控 agent 管理 + 主控终端镜像)③单机控制台(`?m=&s=` 单被控终端)。纯前端,全高撑满,后端零改动。

**Architecture:** `console.html` 同页按 URL 参数 `detectMode` 分多机(无参)/单机(`?m=&s=`)两模式,各模式显隐 hero/term 两组。扇出从控制台迁移到看板(选被控卡片 + 底部 bar `broadcast`)。全高 flex shell(`100dvh`)撑满不塌陷。

**Tech Stack:** 原生 JS(UMD `.cjs` 模块,`window.BoardRender` / `window.CCDashboard`)+ `node:test` + Express hub(后端零改动)。

**Spec:** `docs/superpowers/specs/2026-07-05-7685-command-deck-design.md`
**Mockup:** `docs/superpowers/specs/mockups/{dashboard-multiselect,console-crosspage-fanout,console-single-agent}.html`

**测试基线:** `node --test test/*.test.cjs` 当前 `fail 0`(全绿)。每 task 保持全绿。

---

## File Structure

| 文件 | 职责 | 本 plan 改动 |
|---|---|---|
| `public/board_render.cjs` | 看板纯函数(UMD) | `.card__select` 加多选语义;新增 `groupByMachine` |
| `public/dashboard.css` | 看板 + 控制台共用样式 | 加 `.machine-group`/`.fanout-bar`;控制台撑满(`#ma-screen`/`.console-term` `flex:1`) |
| `public/dashboard.js` | 看板轮询 + 渲染 | `renderBoard` 按机分节;多选 `Set` + click 委托;`#fanout-bar` 逻辑 |
| `public/dashboard.html` | 看板页 | 加 `#fanout-bar`;移除 `⇄switchTab`(看板无切换) |
| `public/console.js` | 控制台逻辑 | 新增 `detectMode`;砍扇出(`selected`/broadcast/`multiSelectMode`/跨页 flag) |
| `public/console.html` | 控制台页 | `#ma-screen` 提为 `.console-app` 子元素;砍 `#bc-count`/`#bc-result` |
| `test/board_render.test.cjs` | board_render 单测 | 更新 card__select 断言;加 `groupByMachine` 测试 |
| `test/dashboard_three_page.test.cjs` | 新建 | renderBoard 分节 / 多选 / fanout payload / detectMode 契约 |
| `test/console_style.test.cjs` | CSS 锁 | 加 `.machine-group`/`.fanout-bar` 存在性锁 |
| `test/console_html.test.cjs` | HTML 锁 | 更新 `#ma-screen` 位置;砍 `#bc-count`/`#bc-result` |

---

## Task 1: board_render.cjs — `.card__select` 多选语义 + `groupByMachine`

**Files:**
- Modify: `public/board_render.cjs:76`(`.card__select` span)、`:163`(export)
- Test: `test/board_render.test.cjs:58-64`(更新)+ 新增 groupByMachine 用例

- [ ] **Step 1: 更新现有测试 + 写新失败测试**

`test/board_render.test.cjs:58-64` 把"纯视觉无交互"断言改为"多选 checkbox 语义",并在文件末尾追加 `groupByMachine` 用例:

```javascript
test('buildCardHTML card__select 带多选 checkbox 语义(data-toggle/role/aria-checked),初始 ☐ 未选', () => {
  const html = B.buildCardHTML({ id: 'm1', name: 'a', online: true }, { name: 's', status: 'idle' }, {});
  assert.match(html, /<span class="card__select" data-toggle="select" role="checkbox" aria-checked="false"[^>]*>☐<\/span>/);
  assert.doesNotMatch(html, /☑/);            // 初始未选(JS toggle 才变 ☑)
  assert.doesNotMatch(html, /card--selected/); // selected 由 JS 重建时加,非 buildCardInner
});
```

文件末尾追加:

```javascript
// ---- groupByMachine(按机分节,离线机排末尾)----
test('groupByMachine 按 machine.id 分组,保留组内顺序', () => {
  const cards = [
    { machine: { id: 'A', online: true },  session: { name: 's1' }, key: 'A/s1' },
    { machine: { id: 'B', online: true },  session: { name: 's2' }, key: 'B/s2' },
    { machine: { id: 'A', online: true },  session: { name: 's3' }, key: 'A/s3' },
  ];
  const groups = B.groupByMachine(cards);
  assert.equal(groups.length, 2);
  assert.equal(groups[0].machine.id, 'A');
  assert.deepEqual(groups[0].cards.map(c => c.key), ['A/s1', 'A/s3']);
  assert.equal(groups[1].machine.id, 'B');
});
test('groupByMachine 离线机组排末尾', () => {
  const cards = [
    { machine: { id: 'off', online: false }, session: { name: 's' }, key: 'off/s' },
    { machine: { id: 'on', online: true },   session: { name: 's' }, key: 'on/s' },
  ];
  const groups = B.groupByMachine(cards);
  assert.equal(groups[0].machine.id, 'on');
  assert.equal(groups[1].machine.id, 'off');
});
test('groupByMachine null/空 → []', () => {
  assert.deepEqual(B.groupByMachine(null), []);
  assert.deepEqual(B.groupByMachine([]), []);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/board_render.test.cjs`
Expected: FAIL(`groupByMachine is not a function` + card__select 断言不匹配)

- [ ] **Step 3: 实现 `.card__select` 多选语义**

`public/board_render.cjs:76` 替换:

```javascript
      `<span class="card__select" data-toggle="select" role="checkbox" aria-checked="false" tabindex="0">☐</span>` +
```

- [ ] **Step 4: 实现 `groupByMachine` + export**

在 `public/board_render.cjs` 的 `summarizeFleet` 之后(`return` 之前)加:

```javascript
  // 按机分节:cards(已 sortCardsByRelevance 排序)→ 按 machine.id 分组;离线机组排末尾。
  // 组内顺序保留(排序已定);返回 [{machine, cards:[]}]。纯函数。
  function groupByMachine(cards) {
    const order = [];
    const map = new Map();
    for (const c of (cards || [])) {
      const mid = c && c.machine && c.machine.id;
      if (mid == null) continue;
      if (!map.has(mid)) { map.set(mid, { machine: c.machine, cards: [] }); order.push(mid); }
      map.get(mid).cards.push(c);
    }
    const online = [], offline = [];
    for (const mid of order) {
      const g = map.get(mid);
      (g.machine && g.machine.online === false ? offline : online).push(g);
    }
    return online.concat(offline);
  }
```

`:163` export 行追加 `groupByMachine`:

```javascript
  return { statusMeta, escapeHtml, relativeTime, buildCardHTML, buildCardInner, flattenFleet, sortCardsByRelevance, summarizeFleet, isStale, partitionStale, groupByMachine };
```

- [ ] **Step 5: 跑测试确认通过**

Run: `node --test test/board_render.test.cjs`
Expected: PASS(全绿)

- [ ] **Step 6: 跑全量确认无回归**

Run: `node --test test/*.test.cjs 2>&1 | tail -3`
Expected: `fail 0`

- [ ] **Step 7: 提交**

```bash
git add public/board_render.cjs test/board_render.test.cjs
git commit -m "feat(board): card__select 多选语义 + groupByMachine 按机分节(三页面 §4.3)"
```

---

## Task 2: dashboard.css — 看板新样式(`.machine-group`/`.fanout-bar`)+ 撑满

**Files:**
- Modify: `public/dashboard.css`(末尾追加)
- Test: `test/console_style.test.cjs`(加 CSS 锁)

- [ ] **Step 1: 写失败测试(CSS 存在性锁)**

`test/console_style.test.cjs` 末尾追加(参考现有锁测试读 `public/dashboard.css` + `assert.match` 的风格):

```javascript
test('三页面样式:存在 .machine-group / .fanout-bar / .card__select[aria-checked] 规则', () => {
  const css = require('fs').readFileSync(require('path').join(__dirname, '..', 'public', 'dashboard.css'), 'utf8');
  assert.match(css, /\.machine-group\b/);
  assert.match(css, /\.machine-group--offline\b/);
  assert.match(css, /\.fanout-bar\b/);
  assert.match(css, /\.card__select\[aria-checked="true"\]/);
  assert.match(css, /\.card__select\b[^}]*min-height:\s*44/);   // 触摸目标 44px(WCAG 2.5.5)
  assert.match(css, /#ma-screen\s*\{[^}]*flex:\s*1/);           // 主控终端撑满
  assert.match(css, /#ma-screen\s*\{[^}]*max-height:\s*none/);  // 覆盖原 max-height:0,否则 flex:1 被锁死
  // 注:.console-term flex:1 由 Task 6 加(与 detectConsoleMode 同提交),此处不断言
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/console_style.test.cjs`
Expected: FAIL(规则不存在)

- [ ] **Step 3: 实现样式**

`public/dashboard.css` 末尾追加(变量复用 mockup 已验证的值,见 `mockups/dashboard-multiselect.html`):

```css
/* === 三页面:看板按机分节 === */
.machine-group{margin-bottom:14px}
.machine-group > details > summary,
.machine-group__title{list-style:none;cursor:pointer;display:flex;align-items:center;gap:8px;
  font-size:.8em;color:var(--fg-2);padding:6px 4px;min-height:44px}
.machine-group > details > summary::-webkit-details-marker{display:none}
.machine-group__caret{display:inline-block;width:1em;color:var(--fg-3);text-align:center}
.machine-group__name{font-weight:600;color:var(--fg)}
.machine-group__count{font-family:var(--mono);font-size:.85em;background:var(--surface-2);
  padding:1px 8px;border-radius:var(--r-pill);color:var(--fg-2)}
.machine-group--offline .machine-group__name{color:var(--fg-3);font-weight:400}
.machine-group--offline .machine-group__count{background:var(--surface-3)}
.machine-group .board-grid{margin-top:4px}

/* 卡片多选交互态(选中由 JS 加 card--selected + aria-checked=true) */
.card__select{cursor:pointer;min-width:44px;min-height:44px;display:inline-flex;align-items:center;justify-content:center}
.card__select:hover{background:var(--accent-bg);border-radius:var(--r-xs)}
.card__select[aria-checked="true"]{color:var(--accent-2)}

/* === 三页面:看板扇出 bar(shell 内 flex 子元素,非 fixed)=== */
.fanout-bar{flex-shrink:0;display:flex;align-items:center;gap:10px;padding:10px 16px;
  padding-bottom:calc(10px + env(safe-area-inset-bottom));background:var(--surface);
  border-top:1px solid var(--border)}
.fanout-bar .sel-count{font-weight:700;color:var(--accent-2);flex-shrink:0}
.fanout-bar .sel-clear{min-height:44px;padding:0 12px;border:1px solid var(--border-2);
  border-radius:var(--r);background:transparent;color:var(--fg-2);font:inherit;font-size:13px;cursor:pointer;flex-shrink:0}
.fanout-bar .term-input{flex:1;min-height:44px;padding:8px 12px;border:1px solid var(--border-2);
  border-radius:var(--r-sm);font:inherit;background:var(--bg-2);color:var(--fg)}
.fanout-bar .term-input:focus{outline:2px solid var(--accent-2);outline-offset:1px;border-color:var(--accent-2)}
.fanout-bar .term-badge{font-size:.75em;background:var(--accent-2);color:#fff;padding:3px 10px;
  border-radius:var(--r-pill);font-weight:600;flex-shrink:0}
.fanout-bar .bc-result{font-size:.75em;color:var(--working);flex-shrink:0;font-family:var(--mono)}

/* === 三页面:控制台撑满(多机 ma-screen / 单机 console-term 各 flex:1)=== */
.console-hero{flex-shrink:0}
#ma-screen{flex:1;min-height:0;max-height:none;overflow:auto;margin:0;padding:12px 16px;border-radius:0}
/* .console-term flex:1 挪到 Task 6(与 detectConsoleMode 同提交)—— 此处先不加,否则 Task 2-6 中间态 /console.html 多机模式 term 会吃光剩余高度、hero 被挤瘪 */
.console-term[data-collapsed="true"]{flex:0 0 auto} /* 折叠态不撑满 */
```

注:`#ma-screen` 现 `.console-hero` 内嵌(Task 7 提为 `.console-app` 直接子元素后,`flex:1` 才生效)。**评审修正**:(a) 真实 `dashboard.css:156` 有 `#ma-screen{max-height:0}`(浮层态默认塌陷),Task 7 提位后 `:157` 浮层选择器失效但 `:156` 的 `max-height:0` 仍生效 → 锁死 `flex:1` 撑满但不可见,故上面 CSS 必须 `max-height:none`(已加)+ 删 `dashboard.css:157` 浮层规则。(b) 真实 `dashboard.css:230-231` 的 `#bc-result`/`.term-badge` id 规则特异性(0,1,0,0)高于本块 `.fanout-bar .bc-result`(0,0,2,0),首屏色偏(JS inline style 最终覆盖);执行时顺手降级或删那两条 id 规则。

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test test/console_style.test.cjs`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add public/dashboard.css test/console_style.test.cjs
git commit -m "style(board): machine-group/fanout-bar 样式 + 控制台撑满 flex:1(三页面 §4.0/§4.3)"
```

---

## Task 3: dashboard.js — `renderBoard` 按机分节 + `<details>` open 继承

**Files:**
- Modify: `public/dashboard.js:142-189`(`renderBoard`)
- Test: `test/dashboard_three_page.test.cjs`(新建)

- [ ] **Step 1: 写失败测试**

新建 `test/dashboard_three_page.test.cjs`:

```javascript
const { test } = require('node:test');
const assert = require('node:assert/strict');
const B = require('../public/board_render.cjs');

// renderBoard 是 DOM 操作函数(在 dashboard.js IIFE 内,无法直接 require)。
// 此处测其依赖的纯契约:groupByMachine 输出 → 分节结构可由 dashboard.js 正确渲染。
// 完整 DOM 行为由 Task 8 浏览器手测 + console_html 锁覆盖。

test('契约:flatten→sort→partition→groupByMachine 链路产出按机分组(离线排末尾)', () => {
  const machines = [
    { id: 'A', name: 'host-A', online: true, sessions: [
      { name: 's1', status: 'working', lastTs: 100 }, { name: 's2', status: 'idle', lastTs: 50 } ] },
    { id: 'B', name: 'host-B', online: false, sessions: [ { name: 's3', status: 'idle', lastTs: 10 } ] },
  ];
  const flat = B.flattenFleet(machines);
  const sorted = B.sortCardsByRelevance(flat);
  const part = B.partitionStale(sorted);
  const groups = B.groupByMachine(part.active);
  assert.equal(groups.length, 2);
  assert.equal(groups[0].machine.id, 'A');   // 在线机组在前
  assert.equal(groups[0].cards.length, 2);
  assert.equal(groups[1].machine.id, 'B');   // 离线机组末尾
  assert.equal(groups[1].machine.online, false);
});
```

- [ ] **Step 2: 跑测试确认通过(纯函数已就绪,此为契约锚点)**

Run: `node --test test/dashboard_three_page.test.cjs`
Expected: PASS(Task 1 的 groupByMachine 已实现)

- [ ] **Step 3: 实现 `renderBoard` 按机分节**

`public/dashboard.js:142-189` 整个 `renderBoard` 替换为:

```javascript
    function renderBoard(payload) {
        var machines = payload.machines || [];
        var flat = BR.flattenFleet(machines);
        var sorted = BR.sortCardsByRelevance(flat);
        var partition = BR.partitionStale(sorted);
        if (machines.length === 0) {
            boardBody.innerHTML = '<li class="board-empty"><span class="eyebrow">NO MACHINES</span> 尚无机器注册到 hub</li>';
            renderFleetSummary(machines);
            return;
        }
        if (!sorted.length) {
            boardBody.innerHTML = '<li class="board-empty"><span class="eyebrow">NO SESSIONS</span> 暂无运行中的会话,在控制台启动一个。</li>';
            renderFleetSummary(machines);
            return;
        }
        // 展开状态继承:全量重建前记录每个 machine-group 与 stale 区的 open,重建后恢复(免轮询重折叠)
        var prevOpen = {}; // mid → true / '__stale__' → true
        var prevDetails = boardBody.querySelectorAll('details[data-mid]');
        Array.prototype.forEach.call(prevDetails, function (d) { if (d.open) prevOpen[d.dataset.mid] = true; });
        var prevStale = boardBody.querySelector('li.board-stale-group > details');
        if (prevStale && prevStale.open) prevOpen['__stale__'] = true;

        boardBody.innerHTML = '';
        // 按机分节:每机 <li class="machine-group"><details><summary>{name} · {n}</summary><ul.board-grid>…
        var groups = BR.groupByMachine(partition.active);
        for (var gi = 0; gi < groups.length; gi++) {
            var g = groups[gi];
            var online = g.machine.online !== false;
            var groupLi = document.createElement('li');
            groupLi.className = 'machine-group' + (online ? '' : ' machine-group--offline');
            var details = document.createElement('details');
            details.dataset.mid = g.machine.id;
            // 在线机默认展开、离线机默认折叠;用户 toggle 过则按记忆(prevOpen)恢复
            details.open = (g.machine.id in prevOpen) ? !!prevOpen[g.machine.id] : online;
            var sum = document.createElement('summary');
            sum.innerHTML = '<span class="machine-group__caret">' + (details.open ? '▼' : '▸') + '</span>' +
                '<span class="machine-group__name">' + (g.machine.name || g.machine.id) + (online ? '' : ' · offline') + '</span>' +
                '<span class="machine-group__count">' + g.cards.length + '</span>';
            var grid = document.createElement('ul');
            grid.className = 'board-grid';
            for (var ci = 0; ci < g.cards.length; ci++) {
                grid.appendChild(buildCardLi(g.cards[ci]));
            }
            details.appendChild(sum);
            details.appendChild(grid);
            groupLi.appendChild(details);
            boardBody.appendChild(groupLi);
        }
        if (partition.stale.length) {
            var groupLi2 = document.createElement('li');
            groupLi2.className = 'board-stale-group';
            var details2 = document.createElement('details');
            details2.dataset.mid = '__stale__';
            if (prevOpen['__stale__']) details2.open = true;
            var sum2 = document.createElement('summary');
            sum2.textContent = partition.stale.length + ' 个陈旧会话(>24h)';
            var grid2 = document.createElement('ul');
            grid2.className = 'board-grid board-stale-grid';
            for (var si = 0; si < partition.stale.length; si++) grid2.appendChild(buildCardLi(partition.stale[si]));
            details2.appendChild(sum2); details2.appendChild(grid2);
            groupLi2.appendChild(details2);
            boardBody.appendChild(groupLi2);
        }
        reapplySelected();           // Task 4:重建后重标选中卡片
        renderFleetSummary(machines);
    }
```

注:`reapplySelected()` 在 Task 4 定义;Task 3 先在文件顶部加 `function reapplySelected(){}` 占位(空函数),Task 4 填充。

- [ ] **Step 4: 跑全量确认无回归**

Run: `node --test test/*.test.cjs 2>&1 | tail -3`
Expected: `fail 0`

- [ ] **Step 5: 提交**

```bash
git add public/dashboard.js test/dashboard_three_page.test.cjs
git commit -m "feat(board): renderBoard 按机分节 + details open 继承扩展(三页面 §4.3①)"
```

---

## Task 4: dashboard.js — 卡片多选(`selected` Map + click 委托 + 重建重标)

**Files:**
- Modify: `public/dashboard.js`(IIFE 内顶部加 `selected` + click 委托;填充 `reapplySelected`)

- [ ] **Step 1: 写失败测试**

`test/dashboard_three_page.test.cjs` 追加(多选 key 契约 + reduce 契约):

```javascript
test('契约:多选 key = `${m.id}/${s.name}`(与 flattenFleet card.key 一致)', () => {
  const flat = B.flattenFleet([{ id: 'A', name: 'a', online: true, sessions: [{ name: 's1', status: 'working' }] }]);
  assert.equal(flat[0].key, 'A/s1');
});

test('契约:broadcast_result.results reduce → {total,succeeded,failed}', () => {
  const results = [
    { target: { machine: 'A', session: 's1' }, ok: true },
    { target: { machine: 'A', session: 's2' }, ok: false, error: 'offline' },
    { target: { machine: 'B', session: 's3' }, ok: true },
  ];
  const total = results.length;
  const succeeded = results.filter(r => r.ok).length;
  assert.equal(total, 3);
  assert.equal(succeeded, 2);
  assert.equal(total - succeeded, 1);
});
```

- [ ] **Step 2: 跑测试确认通过(契约锚点)**

Run: `node --test test/dashboard_three_page.test.cjs`
Expected: PASS

- [ ] **Step 3: 实现多选状态 + click 委托**

`public/dashboard.js` 在 `var boardStale = ...`(:111 附近)之后加(用 `Map<key,{machine,session}>` 直存对象,免 `split('/')` 截断):

```javascript
    // 三页面:卡片多选(扇出目标)。key = `${m.id}/${s.name}`(card.key)。Map 跨 2s 重建保持。
    var selected = new Map();   // key → {machine,session}
    function keyOf(machineId, sessionName) { return machineId + '/' + sessionName; }
    // Q3:sessionStorage 持久化选中态(跳转单机控制台返回后恢复)。存完整 {machine,session} 对象数组,免 split('/') 截断。
    var SELECTED_KEY = 'ccBoardSelected';
    function persistSelected() {
        try { sessionStorage.setItem(SELECTED_KEY, JSON.stringify(Array.from(selected.values()))); } catch (e) { /* sessionStorage 不可用时静默 */ }
    }
    function loadSelected() {
        try {
            var raw = sessionStorage.getItem(SELECTED_KEY);
            if (!raw) return;
            var arr = JSON.parse(raw);
            if (!Array.isArray(arr)) return;
            selected = new Map();   // reassign 闭包变量(后续 click 委托/reapplySelected 读到新 Map)
            for (var i = 0; i < arr.length; i++) {
                var t = arr[i];
                if (t && t.machine != null && t.session != null) selected.set(keyOf(t.machine, t.session), { machine: t.machine, session: t.session });
            }
        } catch (e) { /* 损坏 JSON 静默丢弃 */ }
    }
    loadSelected();   // 初始化:首次 renderBoard 前填充 Map(reapplySelected 自动重标 DOM)
    function reapplySelected() {
        // 全量重建后 selected(Map)存活,重标 DOM 的 card--selected + aria-checked
        var cards = boardBody.querySelectorAll('.card');
        Array.prototype.forEach.call(cards, function (a) {
            var key = keyOf(a.getAttribute('data-machine'), a.getAttribute('data-session'));
            if (selected.has(key)) {
                a.classList.add('card--selected');
                var tog = a.querySelector('.card__select');
                if (tog) { tog.setAttribute('aria-checked', 'true'); tog.textContent = '☑'; }
            }
        });
        updateFanoutBar();
    }
```

click 委托(在 `var boardBody = ...` 之后绑定一次):

```javascript
    // click 委托:命中 .card__select[data-toggle] → toggle 选中(preventDefault 阻止 <a> 跳转);
    // 其余区域放行 → <a href> 原生跳单机控制台(console.html?m=&s=)
    boardBody.addEventListener('click', function (e) {
        var tog = e.target.closest('[data-toggle="select"]');
        if (!tog) return;
        e.preventDefault();
        var card = tog.closest('.card');
        if (!card) return;
        var key = keyOf(card.getAttribute('data-machine'), card.getAttribute('data-session'));
        if (selected.has(key)) {
            selected.delete(key);
            card.classList.remove('card--selected');
            tog.setAttribute('aria-checked', 'false'); tog.textContent = '☐';
        } else {
            if (selected.size >= 50) { return; }   // 后端 BROADCAST_MAX_TARGETS=50 上限,阻止继续选
            selected.set(key, { machine: card.getAttribute('data-machine'), session: card.getAttribute('data-session') });
            card.classList.add('card--selected');
            tog.setAttribute('aria-checked', 'true'); tog.textContent = '☑';
        }
        updateFanoutBar();
    });
    // keydown 委托:键盘可达性(WCAG 2.1.1)—— role=checkbox 上 Enter/Space 不自动派发 click,
    // 显式拦截并复用同一 toggle 逻辑(触发 .click() 走上面的 click 委托)。
    boardBody.addEventListener('keydown', function (e) {
        if ((e.key === 'Enter' || e.key === ' ') && e.target.closest('[data-toggle="select"]')) {
            e.preventDefault();
            e.target.click();
        }
    });
```

`updateFanoutBar` Task 5 填充;Task 4 先占位 `function updateFanoutBar(){}`。

- [ ] **Step 4: 跑全量确认无回归**

Run: `node --test test/*.test.cjs 2>&1 | tail -3`
Expected: `fail 0`

- [ ] **Step 5: 提交**

```bash
git add public/dashboard.js test/dashboard_three_page.test.cjs
git commit -m "feat(board): 卡片多选 selected Map + click 委托 + 重建重标(三页面 §4.3②)"
```

---

## Task 5: dashboard.html + dashboard.js — 扇出 bar(`broadcast` + results reduce)

**Files:**
- Modify: `public/dashboard.html`(加 `#fanout-bar`,移除 `⇄switchTab` + 其 `<script>`)
- Modify: `public/dashboard.js`(填充 `updateFanoutBar` + fanout submit + `broadcast_result` 处理 + 复用 hub ws)

- [ ] **Step 1: 写失败测试(broadcast payload 契约)**

`test/dashboard_three_page.test.cjs` 追加:

```javascript
test('契约:扇出 WS payload = {type:broadcast, targets:[{machine,session}], data, enter:true}', () => {
  var selected = new Map();
  selected.set('A/s1', { machine: 'A', session: 's1' });
  selected.set('B/s2', { machine: 'B', session: 's2' });
  var payload = { type: 'broadcast', targets: Array.from(selected.values()), data: 'ls', enter: true };
  assert.equal(payload.type, 'broadcast');
  assert.equal(payload.targets.length, 2);
  assert.deepEqual(payload.targets[0], { machine: 'A', session: 's1' });
  assert.equal(payload.enter, true);
});
```

- [ ] **Step 2: 跑测试确认通过**

Run: `node --test test/dashboard_three_page.test.cjs`
Expected: PASS

- [ ] **Step 3: dashboard.html 加 `#fanout-bar` + 移除 `⇄switchTab`**

`public/dashboard.html` 在 `</main>`(:33)与 `.bottom-tabbar`(:35)之间插入:

```html
        <!-- 三页面:扇出 bar(选中卡片后显示,Enter 广播,不跳控制台)-->
        <form id="fanout-bar" class="fanout-bar" hidden>
            已选 <span class="sel-count" id="sel-count">0</span>
            <button class="sel-clear" id="sel-clear" type="button">清除</button>
            <input class="term-input" id="fanout-input" placeholder="输入(Enter 扇出)…" autocomplete="off" aria-label="扇出指令输入" />
            <span class="term-badge" id="bc-count" hidden></span>
            <span class="bc-result" id="bc-result" aria-live="polite"></span>
        </form>
```

`.bottom-tabbar`(:35-39)移除 `⇄switchTab` 按钮(看板无切换终端,扇出在本页):

```html
        <nav class="bottom-tabbar" aria-label="主导航">
            <a class="tab" id="tab-console" href="/"><span class="tab-icon" aria-hidden="true">▤</span><span class="tab-label">控制台</span></a>
            <a class="tab tab--active" href="/dashboard.html" aria-current="page"><span class="tab-icon" aria-hidden="true">◫</span><span class="tab-label">看板</span></a>
        </nav>
```

`switchTab` `<script>`(:41-48)整块删除(跨页 flag 协议废弃,扇出归看板)。

- [ ] **Step 4: dashboard.js 填充 fanout 逻辑**

`public/dashboard.js` 在多选 click 委托后加(fanout bar DOM 引用 + ws + submit):

```javascript
    // ---- 三页面:扇出 bar(broadcast + broadcast_result reduce)----
    var fanoutBar = document.getElementById('fanout-bar');
    var selCount = document.getElementById('sel-count');
    var fanoutInput = document.getElementById('fanout-input');
    var fanoutBcCount = document.getElementById('bc-count');
    var fanoutBcResult = document.getElementById('bc-result');
    var hubWs = null;
    function hubWsUrl() {
        var proto = location.protocol === 'https:' ? 'wss' : 'ws';
        var tok = new URLSearchParams(location.search).get('token');   // 复用 console.js:16-17 同款 ?token= 拼接(直链/测试 fallback,cookie 不够用时兜底)
        return proto + '://' + location.host + '/' + (tok ? '?token=' + encodeURIComponent(tok) : '');
    }
    function ensureHubWs() {
        if (hubWs && hubWs.readyState <= 1) return hubWs;
        hubWs = new WebSocket(hubWsUrl());
        hubWs.onmessage = function (ev) {
            var msg; try { msg = JSON.parse(ev.data); } catch (e) { return; }
            if (msg.type === 'broadcast_result') {
                var arr = Array.isArray(msg.results) ? msg.results : [];
                var okN = arr.filter(function (r) { return r.ok; }).length;
                fanoutBcResult.textContent = '成功 ' + okN + '/' + arr.length;
                fanoutBcResult.style.color = okN === arr.length ? 'var(--working)' : 'var(--errored)';
            }
        };
        hubWs.onclose = function () { fanoutBcResult.textContent = '连接断开,重试…'; fanoutBcResult.style.color = 'var(--errored)'; };
        hubWs.onerror = function () { fanoutBcResult.textContent = '连接失败,检查 token/网络'; fanoutBcResult.style.color = 'var(--errored)'; };
        return hubWs;
    }
    function sendHub(msg) {
        var payload = JSON.stringify(msg);
        if (hubWs && hubWs.readyState === 1) hubWs.send(payload);
        else if (hubWs) hubWs.addEventListener('open', function () { hubWs.send(payload); }, { once: true });
    }
    function updateFanoutBar() {
        var n = selected.size;
        selCount.textContent = n;
        fanoutBar.hidden = n === 0;
        fanoutBcCount.hidden = n < 2;
        fanoutBcCount.textContent = n >= 2 ? ('扇出 ' + n) : '';
        fanoutInput.placeholder = n >= 2 ? ('输入(Enter 扇出给 ' + n + ' 个被控)…')
            : n === 1 ? '输入(Enter 发送给该被控)…' : '输入…';
        persistSelected();   // Q3:所有 toggle/clear 路径都经 updateFanoutBar,集中持久化(reapplySelected 调时写回相同值,幂等无害)
    }
    if (fanoutBar) {
        ensureHubWs();
        fanoutBar.addEventListener('submit', function (e) {
            e.preventDefault();
            var v = fanoutInput.value;
            if (!v || selected.size === 0) { return; }
            ensureHubWs();
            var targets = Array.from(selected.values());
            fanoutBcResult.textContent = '扇出中…';
            fanoutBcResult.style.color = 'var(--waiting)';
            sendHub({ type: 'broadcast', targets: targets, data: v, enter: true });
            fanoutInput.value = '';
        });
        document.getElementById('sel-clear').addEventListener('click', function () {
            selected.clear();
            Array.prototype.forEach.call(boardBody.querySelectorAll('.card--selected'), function (a) {
                a.classList.remove('card--selected');
                var tog = a.querySelector('.card__select');
                if (tog) { tog.setAttribute('aria-checked', 'false'); tog.textContent = '☐'; }
            });
            updateFanoutBar();
        });
    }
```

- [ ] **Step 5: 跑全量确认无回归**

Run: `node --test test/*.test.cjs 2>&1 | tail -3`
Expected: `fail 0`(注意:`dashboard_tabbar.test.cjs`/`dashboard-dual-mode.test.cjs` 可能断言 switchTab 存在 → 需同步更新,见 Step 6)

- [ ] **Step 6: 修复受影响测试**

**必更新**:`dashboard_tabbar.test.cjs:13` 断言 `id="switchTab"`(看板移除该按钮 → fail);`:22-28` 断言 `sessionStorage.setItem('openSwitchSheet','1')` + `location='/'`(跨页 flag 废弃 → fail,且 `:22-28` 用 `openSwitchSheet` 字面量,grep `switchTab` **抓不到**)。`dashboard-dual-mode.test.cjs` 同查。grep 确认:`grep -rn 'openSwitchSheet\|switchTab' test/dashboard_tabbar.test.cjs test/dashboard-dual-mode.test.cjs`,把看板侧这些断言整体删除(看板不再写 sessionStorage/跳 /、switchTab 已移除)。

- [ ] **Step 7: 提交**

```bash
git add public/dashboard.html public/dashboard.js test/dashboard_three_page.test.cjs test/dashboard_tabbar.test.cjs test/dashboard-dual-mode.test.cjs
git commit -m "feat(board): 扇出 bar broadcast + results reduce,看板移除 switchTab(三页面 §4.3③/§4.4)"
```

---

## Task 6: console.js — `detectConsoleMode` + 砍扇出

**Files:**
- Modify: `public/console.js`(新增 `detectMode`;砍 `:20 selected`、`:29-30 bcCount/bcResult`、`:78-82 broadcast_result`、`:119 selected.clear`、`:132-140 广播分支`、`:149-154 refreshBroadcast`、`:287 multiSelectMode`、`:345/369/371-377/386-395 抽屉多选`、`:424-434 跨页 flag`)
- Test: `test/dashboard_three_page.test.cjs`(detectMode 契约)

- [ ] **Step 1: 写失败测试(detectMode 契约)**

`test/dashboard_three_page.test.cjs` 追加:

```javascript
test('契约:控制台 detectMode —— ?m=&s= 存在 → single;否则 multi', () => {
  const hasParam = (qs) => !!(new URLSearchParams(qs).get('m') && new URLSearchParams(qs).get('s'));
  assert.equal(hasParam('m=A&s=s1'), true);   // single
  assert.equal(hasParam(''), false);           // multi
  assert.equal(hasParam('m=A'), false);        // 缺 s → multi
});
```

- [ ] **Step 2: 跑测试确认通过**

Run: `node --test test/dashboard_three_page.test.cjs`
Expected: PASS

- [ ] **Step 3: 砍扇出 —— 删除 broadcast 相关代码**

`public/console.js`:
- `:20` `const selected = new Map();` → 删
- `:29-30` `const bcCount`/`const bcResult` → 删
- `:78-82` ws.onmessage 的 `broadcast_result` 分支 → 删
- `:119` `attachTarget` 内 `selected.clear(); refreshBroadcast();` → 删
- `:128-147` `termForm submit`:删 `selected.size >= 2`(broadcast)与 `selected.size === 1` 两分支,保留 `currentTarget` 单发:

```javascript
  termForm.addEventListener('submit', (e) => {
    e.preventDefault();
    if (!termInput.value) return;
    ensureWs();
    if (currentTarget) {
      sendWhenOpen({ type: 'input', target: currentTarget, data: termInput.value, enter: true });
    }
    termInput.value = '';
  });
```

- `:149-154` `refreshBroadcast` 整个函数 → 删
- `:287` `let multiSelectMode = false;` → 删
- `:345` `title.textContent` 多选分支 → 简化为 `title.textContent = '机器';`
- `:364` `const isSel = selected.has(it.key);` → **删**(`selected` 已删,保留会 ReferenceError —— 评审发现 plan/spec 原均漏列此行)
- `:367` `btn.setAttribute('aria-pressed', String(isSel));` → 改基于 currentTarget 判等:`btn.setAttribute('aria-pressed', String(it.machine===currentTarget.machine && it.session===currentTarget.session));`
- `:369` btn.textContent `(multiSelectMode ? (isSel?'☑':'☐') : '') + it.label` → `it.label`
- `:371-377` click 内 `if (multiSelectMode){...}` 分支 → 删,只留 `attachTarget(...); switchSheet.close();`
- `:386-395` multitoggle 按钮 → 删
- `:424-434` 跨页 `openSwitchSheet` flag 整块 → 删

- [ ] **Step 4: 实现 `detectConsoleMode`**

`public/console.js` 在 `tryAttachFromUrl` 之后定义,末尾 init 改由 detectMode 分发:

```javascript
  // ---- 三页面:detectConsoleMode 按 ?m=&s= 切 hero/term 显隐 ----
  // 评审修正:(a) 复用 console.js:34 已有 maPanel / :37 maScreen(同 DOM),不新引 heroPanel/maScreenEl;
  //         (b) 改名 detectConsoleMode,避开 dashboard.js:234 已有的 detectMode(hub 探测)撞名;
  //         (c) multi 分支 ma-screen 是主显示区,须 aria-hidden=false 否则读屏跳过。
  function detectConsoleMode() {
    const single = !!(urlM && urlS);   // urlM/urlS 已在 IIFE 顶层(console.js:406-407),闭包可直接用
    if (single) {
      // 单机控制台:显示 term + switchTab,隐藏 hero + ma-screen
      if (maPanel) maPanel.hidden = true;
      if (maScreen) { maScreen.hidden = true; maScreen.setAttribute('aria-hidden', 'true'); }
      if (termSection) termSection.hidden = false;
      if (switchTab) switchTab.hidden = false;
      tryAttachFromUrl();
    } else {
      // 多机控制台:显示 hero + ma-screen,隐藏 term + switchTab
      if (maPanel) maPanel.hidden = false;
      if (maScreen) { maScreen.hidden = false; maScreen.setAttribute('aria-hidden', 'false'); }
      if (termSection) termSection.hidden = true;
      if (switchTab) switchTab.hidden = true;
    }
    return single ? 'single' : 'multi';
  }
```

末尾 init(`:447-449`)改为:

```javascript
  setInterval(renderMaCallout, 30000);
  const mode = detectConsoleMode();
  if (mode === 'multi') {
    setInterval(poll, 2000);
    poll();
    ensureMaWs();                     // 多机模式才确保主控 ws
  } else {
    ensureWs();                       // 单机模式确保 term ws(tryAttachFromUrl 内已调,幂等)
  }
  renderTopbarAlert();
```

注:评审修正 —— `urlM`/`urlS` **已是 IIFE 顶层变量**(console.js:406-407,非 `tryAttachFromUrl` 内局部),`detectConsoleMode` 闭包可直接读,**无需"改为模块顶部"**(原 plan 那句是预判错误,删掉,否则执行者重复声明会 SyntaxError)。`termSection` 为现有 DOM 引用;`switchTab` 需在 Task 6 顶部加 `const switchTab = document.getElementById('switchTab');`(Task 7 给该按钮加 `hidden` 属性)。

- [ ] **Step 4b: dashboard.css `.console-term{flex:1;min-height:0}`(与 detectConsoleMode 同提交)**

评审时序修正:`.console-term` 是 `.console-app` 直接子元素。若在 Task 2 就改 `flex:1`(原 `flex:0 0 40vh`)会立即生效,但 detectConsoleMode(Task 6)才在多机模式 hidden 掉 term → Task 2-6 中间提交里 `/console.html`(多机)的 term 吃光剩余高度、hero 被挤瘪(`console_style.test.cjs` 只匹配 CSS 文本,抓不到布局塌陷)。故 `flex:1` 改动挪到此处,与 detectConsoleMode 同提交:

```css
.console-term{flex:1;min-height:0}
```

此时多机模式 term 已 `hidden`,`flex:1` 只在单机模式生效,无中间态塌陷。同步在 `test/console_style.test.cjs` 加 `.console-term flex:1` 断言(Task 2 锁已注释掉该断言,此处解开)。

- [ ] **Step 5: 更新受影响的 JS-source 契约测试(CRITICAL)**

`test/console_html.test.cjs:71-108` 有 4 个 test 读 console.js 源码字符串,断言 `selected.size`/`type:'broadcast'`/`多选|multiSelect`(:71-75)、`openSwitchSheet`+`sessionStorage`(:81-84)、`selected.size`/`broadcast`/`input`(:101-105)、`bcCount.hidden = selected.size < 2`(:106-108)。Task 6 删扇出代码后这 4 个 test **全 fail**。**删除这 4 个 test**(扇出协议已挪看板,console 不再有这些行为)。同步检查 `test/switch_sheet.test.cjs` 若断言 multitoggle 切换,一并更新为单选 attach。

**Q4 源码契约**(锁住 `detectConsoleMode` 存在 + init 调用 + submit 单发 `input`/无 `broadcast`,防回归)—— 删完上述 4 test 后,在 `console_html.test.cjs` 末尾追加(仿现有读源码断言风格):

```javascript
test('源码契约:console.js 定义 detectConsoleMode 且 init 分发调用(三页面分模式)', () => {
  const fs = require('fs'), path = require('path');
  const src = fs.readFileSync(path.join(__dirname, '..', 'public', 'console.js'), 'utf8');
  assert.match(src, /function\s+detectConsoleMode\s*\(/);
  assert.match(src, /detectConsoleMode\(\)/);   // 末尾 init 分发调用
});
test('源码契约:console.js submit 单发 type:input,无 broadcast(扇出已挪看板)', () => {
  const fs = require('fs'), path = require('path');
  const src = fs.readFileSync(path.join(__dirname, '..', 'public', 'console.js'), 'utf8');
  assert.match(src, /type:\s*['"]input['"]/);
  assert.doesNotMatch(src, /type:\s*['"]broadcast['"]/);
});
```

Run: `node --test test/console_html.test.cjs test/switch_sheet.test.cjs`
Expected: PASS

- [ ] **Step 5b: 跑全量确认无回归**

Run: `node --test test/*.test.cjs 2>&1 | tail -3`
Expected: `fail 0`

- [ ] **Step 6: 提交**

```bash
git add public/console.js test/dashboard_three_page.test.cjs test/switch_sheet.test.cjs
git commit -m "feat(console): detectMode 三页面分模式 + 砍扇出挪看板(三页面 §4.1/§4.2)"
```

---

## Task 7: console.html — `#ma-screen` 提位 + 砍 `#bc-*` + 撑满

**Files:**
- Modify: `public/console.html:31`(`#ma-screen` 移出 hero)、`:43-46`(`#term-input-form` 砍 `#bc-*`)、`:51-55`(tabbar `⇄` 加 `hidden`)
- Test: `test/console_html.test.cjs`(更新 ma-screen 位置 + 砍 bc 断言)

- [ ] **Step 1: 更新 HTML 锁测试**

`test/console_html.test.cjs`(参考现有断言风格,读 `public/console.html`):
- 原 `#ma-screen` 在 `#main-agent-panel` 内的断言 → 改为"`#ma-screen` 是 `.console-app` 直接子元素(位于 `#main-agent-panel` 之后)"
- `#bc-count`/`#bc-result` 断言 → 删除(已砍)
- 加 `#fanout-bar` 不在 console.html(跳过;fanout 属看板)

具体:grep `console_html.test.cjs` 内 `ma-screen`/`bc-count`/`bc-result` 断言,按上面语义更新。

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test test/console_html.test.cjs`
Expected: FAIL(ma-screen 位置未变)

- [ ] **Step 3: 实现 DOM 调整**

`public/console.html:31` —— 把 `<pre id="ma-screen">…</pre>` 从 `#main-agent-panel`(:21-33 section)内**移出**,放到 `.console-app` 直接子元素位置(`#main-agent-panel` section 之后、`.console-term` section 之前)。hero 内保留 `.ma-warn-line` 等控制按钮。

`public/console.html:43-47` `#term-input-form` 砍 `#bc-count`/`#bc-result`:

```html
      <form id="term-input-form" class="term-input-form">
        <input id="term-input" class="term-input" placeholder="输入(Enter 发送)…" aria-label="指令输入(Enter 发送给当前被控)" autocomplete="off" />
      </form>
```

`public/console.html:51-55` `.bottom-tabbar` —— `⇄switchTab` 加 `hidden`(detectMode 单机模式才显示):

```html
    <nav class="bottom-tabbar" aria-label="主导航">
      <a class="tab tab--active" href="/console.html" aria-current="page"><span class="tab-icon" aria-hidden="true">▤</span><span class="tab-label">控制台</span></a>
      <a class="tab" href="/dashboard.html"><span class="tab-icon" aria-hidden="true">◫</span><span class="tab-label">看板</span></a>
      <button class="tab" type="button" id="switchTab" hidden aria-haspopup="dialog" aria-expanded="false"><span class="tab-icon" aria-hidden="true">⇄</span><span class="tab-label">切换</span></button>
    </nav>
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test test/console_html.test.cjs`
Expected: PASS

- [ ] **Step 5: 跑全量确认无回归**

Run: `node --test test/*.test.cjs 2>&1 | tail -3`
Expected: `fail 0`

- [ ] **Step 6: 提交**

```bash
git add public/console.html test/console_html.test.cjs
git commit -m "feat(console): ma-screen 提位撑满 + 砍 bc-* + switchTab 单机模式(三页面 §4.0/§4.1)"
```

---

## Task 8: 全量验证 + 撑满/三页手测

**Files:** 无(验证 only)

- [ ] **Step 1: 全量测试**

Run: `node --test test/*.test.cjs 2>&1 | tail -5`
Expected: `fail 0`,pass 数 ≥ 基线

- [ ] **Step 2: 起 hub 浏览器手测**

```
! node hub/server.cjs   (或现有启动方式,见 CLAUDE.md / 操作手册)
```
浏览器开 `http://localhost:7685/dashboard.html`(需 token,见 hub 配置):

- **看板**:卡片按机分组(`<details>` 折叠箭头);点 `☐` → `☑` 选中 + 底部扇出 bar 出现;Enter 扇出 → `成功 N/M`;点卡片其余区域 → 跳单机控制台;刷新/等 2s → 选中态保持;**跳转单机控制台后返回看板 → 选中态恢复**(Q3 sessionStorage);页面撑满无底部留白。
- **多机控制台**(`/console.html`):hero(主控 Start/Stop)+ `#ma-screen` 撑满;无 term 输入;`⇄` 隐藏。
- **单机控制台**(`/console.html?m=X&s=Y`):`.console-term` 撑满;`#term-input` 单发;`⇄` 显示,点开抽屉单选 attach;无 hero/ma-screen。

- [ ] **Step 3: 修复手测发现的问题**(若有,回到对应 Task)

- [ ] **Step 4: 最终提交 + 收尾**

```bash
git add -A
git commit -m "test: 三页面重构全量验证通过(fail 0)"
```

使用 `superpowers:finishing-a-development-branch` 收尾(若用 worktree)。

---

## Self-Review(写完后自查)

**Spec 覆盖**:
- §4.0 撑满 → Task 2(CSS)+ Task 7(ma-screen 提位)✅
- §4.1 多机控制台 → Task 6 detectMode multi + Task 7 ma-screen ✅
- §4.2 单机控制台 → Task 6 detectMode single + 保留 attachTarget/tryAttachFromUrl ✅
- §4.3① 按机分节 → Task 1 groupByMachine + Task 3 renderBoard ✅
- §4.3② 卡片多选 → Task 1 card__select 语义 + Task 4 selected/click ✅
- §4.3③ 扇出 bar → Task 5 ✅
- §4.4 扇出协议 → Task 5 broadcast + results reduce ✅
- §3 要砍清单 → Task 6 ✅

**已知简化/风险**:
- `split('/')` 多选 key 风险:改用 `Map<key,{machine,session}>` 直存对象(Task 4)✅
- `renderBoard` open 继承:在线默认展开、离线默认折叠,用户 toggle 跨轮询保持(Task 3)✅
- `dashboard_tabbar`/`dashboard-dual-mode`/`switch_sheet`/`console_html` 受影响测试:Task 5/6/7 Step 6 同步更新 ✅
- `#ma-screen` 提位后 `maToggleBtn`(▾镜像)浮层逻辑失效:Task 7 提位后 ma-screen 常驻撑满(`flex:1`),`data-ma-open` 浮层 CSS 可保留不生效(无害)或后续清理 —— 多机模式 ma-screen 是主显示区,无需折叠。若需保留折叠,改 `maToggleBtn` 切 `ma-screen.hidden`。
- **Q2 checkbox 移出 `<a>`(评审 a11y 建议,经核实未应用 —— 待用户确认)**:评审建议把 `.card__select` 从 `<a>` 内移出做 `<li>` 兄弟(语义更干净,checkbox 非 link 后代,删 preventDefault hack)。2026-07-05 核实真实结构(`buildCardInner` board_render.cjs:75-83 返回整个 `<a>` 内含 `.card__select`;`buildCardLi` dashboard.js:133-141 包 `<li>`)后确认为 **7 处连锁**(buildCardInner 去 span / buildCardLi 加 button+dataset / click 委托去 preventDefault 改读 li.dataset / 删 keydown 委托 / reapplySelected 改读 li+选中态从 a 迁 li / CSS `.card-row` flex+button reset+`.card--selected` 选择器迁移 / board_render 测试断言改),**非原判"轻量"**。当前方案(`<span role="checkbox" tabindex="0" aria-checked>` 在 a 内 + click 委托 preventDefault + keydown 委托)**合法且 WCAG 2.1.1/2.5.5 可达**。**推荐保持当前方案**(性价比;YAGNI);若优先 a11y 语义洁癖,可作后续独立重构(不阻塞三页面上线)。

**类型一致**:`keyOf(mid,sess)` / `selected` Map / `groupByMachine` 返回 `[{machine,cards}]` —— 跨 Task 一致 ✅

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-05-7685-three-page.md`. Two execution options:

**1. Subagent-Driven (recommended)** — I dispatch a fresh subagent per task, review between tasks, fast iteration.

**2. Inline Execution** — Execute tasks in this session using executing-plans, batch execution with checkpoints.

Which approach?
